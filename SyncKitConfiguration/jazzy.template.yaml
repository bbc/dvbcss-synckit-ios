module: SyncKitConfiguration
module_version: 1.0.0
umbrella_header: SyncKitConfiguration/SyncKitConfiguration.h
framework_root: .
output: ./../build-docs/SyncKitConfiguration

# Remaining config comes from when concatenated with common jazzy config in repository root
