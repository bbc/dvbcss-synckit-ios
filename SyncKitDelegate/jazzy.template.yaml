module: SyncKitDelegate
module_version: 1.0.0
umbrella_header: SyncKitDelegate/SyncKitDelegate.h
framework_root: .
output: ./../build-docs/SyncKitDelegate

# Remaining config comes from when concatenated with common jazzy config in repository root
